pragma solidity 0.8.26;
import {SafeCallback} from "v4-periphery/src/base/SafeCallback.sol";

    /*
abstract contract Harvester is SafeCallback {
    constructor(address _manager) SafeCallback(_manager) {}
    function harvest(bytes calldata data) external  returns (bytes4, int128) {
        //Ideally we want to flash account a bunch of accrued fees into the node operators desired payout currency
       return (bytes4(0x123), 1);
    }
        
}
    */